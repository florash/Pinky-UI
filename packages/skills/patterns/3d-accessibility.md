# 3D Accessibility

Every 3D or 2.5D collection needs an understandable flattened alternative. Selection, labels, controls and reading order must work without pointer position, camera orientation, hover or motion.

Use native buttons/links, a roving tab stop only when it clarifies a collection, explicit Previous/Next paths, reduced-motion flattening and touch simplification. Never hide essential information behind depth.
