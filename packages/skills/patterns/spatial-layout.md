# Spatial Layout

Spatial depth is hierarchy, not a camera ride. A spatial collection needs a finite item model, explicit controls, logical DOM order and a complete flat fallback.

Orbit Gallery is intentionally not added in this pack: Orbit Menu already covers radial action placement, while Cylinder Gallery and the existing carousel/gallery systems cover finite media rotation without another overlapping API.
